Think Python by Allen DowneyChapter 3 -  NotesFunctions* Function - a named sequence of statements that perform a computation* ‘int’ function converts values into integers* Float converts integers and strings to floating numbers* Module is a file that contains a collection of related functions* Module object contains the functions and variables defined in the module* Function definition specifies the name of a new function and the sequence of statements that run when the function is called* Header- first line of the function definitiono Header must end with a colon* Arguments are assigned to variables called parameters* Creating a variable inside a function makes it local* Drawing a stack diagram helps keep track of which variables being usedo Represented with frames* List of functions is called a trackback* 